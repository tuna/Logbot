
source 'https://rubygems.org'

gem 'jellyfish' # web
gem 'cinch'     # agent
gem 'redis'     # used in web and agent

# All of below are optional/selectional:
group :server do
  gem 'rainbows'
end

# compile assets
group :assets do
  gem 'compass'
  gem 'sass'
end
